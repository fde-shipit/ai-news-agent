# Get your API key from https://console.anthropic.com → API Keys
# You'll need to create an account and add a payment method first.
ANTHROPIC_API_KEY=sk-ant-...

# Set to false unless you're building a shared/multi-tenant deployment.
# When false, the app uses the key above for all requests.
NEXT_PUBLIC_MULTI_USER=false
